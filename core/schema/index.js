'use strict';

const baseTypes = require('./base-types');
const relationshipTypes = require('./relationship-types');
const schemaTypes = require('./schema-types');

module.exports = {
  baseTypes,
  relationshipTypes,
  schemaTypes
};
